---
sop_id: "SOP-XX"           # ID Único (ex: SOP-01)
title: "Nome do Processo"  # Ex: Business Rules Definition
version: "1.4.0"           # Versão do SOP
phase: 1                   # Fase do Ciclo SODA (1-8)
role: "@analyst"           # Persona responsável (chave p/ AGENTS.md)
type: "procedure"          # procedure | policy | standard

# O CONTRATO DE ENTRADA (O que o agente DEVE ler antes)
inputs:
  required:
    - "PROJECT_CHARTER.md"
  optional:
    - "docs/business/glossary.md"

# O CONTRATO DE SAÍDA (O que o agente DEVE gerar)
outputs:
  - path: "docs/caminho/do/arquivo.md"
    template: "templates/nome_do_template.md"
    validation: "file_exists" # Regra de validação automática

# CONFIGURAÇÃO DE EXECUÇÃO (Para o Ralph Loop)
execution:
  complexity: "low"        # low | medium | high (define timeouts)
  tools:                   # Sandbox de ferramentas permitidas
    - "file_read"
    - "ask_user"
---

# 1. OBJETIVO
[Descrição em texto para o LLM...]